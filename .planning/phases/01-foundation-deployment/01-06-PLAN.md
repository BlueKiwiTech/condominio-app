---
phase: 01-foundation-deployment
plan: 06
type: execute
wave: 4
depends_on: ["04", "05"]
files_modified:
  - .env.example
autonomous: true
requirements: [DPLY-01, DPLY-02]

must_haves:
  truths:
    - "Visiting the deployed Vercel production URL renders the Once UI + next-intl shell without build errors"
    - "Vercel's production environment variables are wired to the actual Supabase project created in Plan 04 (not placeholder values)"
    - "The secret/service-role key never appears in the client-side JavaScript bundle"
    - "main branch pushes deploy directly to production (D-05) — no preview/staging environment exists"
  artifacts:
    - path: ".env.example"
      provides: "Documents the exact env var names (not values) a future developer needs, matching what lib/supabase/*.ts and proxy.ts actually reference"
  key_links:
    - from: "Vercel production env vars"
      to: "the condominio-app Supabase project (Plan 04)"
      via: "vercel env add ... production"
      pattern: "NEXT_PUBLIC_SUPABASE_URL"
    - from: "GitHub main branch"
      to: "Vercel production deployment"
      via: "vercel git connect"
      pattern: "git connect"
---

<objective>
Create the Vercel project, connect it to this repo's GitHub remote for `main`-branch-deploys-to-production (D-05), wire the real Supabase project's env vars (from Plan 04) into Vercel's production environment, deploy, and run the full DPLY-01 verification: the deployed URL renders correctly AND the secret key never leaked into the client bundle (T-01).

Purpose: This is the phase's final, user-visible success criterion — "visiting the deployed Vercel URL renders the Next.js + Once UI component shell without build errors, with environment variables correctly wired to the Supabase project." Everything in Plans 01-05 exists to make this plan's deploy succeed.
Output: A live production Vercel deployment of `condominio-app`, `.env.example` documenting the real env var contract, and confirmed absence of the secret key in the built client bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-deployment/01-CONTEXT.md
@.planning/phases/01-foundation-deployment/01-04-SUMMARY.md
@.planning/phases/01-foundation-deployment/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .env.example with the real, actually-used env var names</name>
  <read_first>
    .planning/phases/01-foundation-deployment/01-04-SUMMARY.md (the actual key-naming convention issued by the created Supabase project)
    lib/supabase/server.ts, proxy.ts (Plan 05 output — confirms exactly which env var names the app code references, so `.env.example` never drifts from reality)
  </read_first>
  <files>.env.example</files>
  <action>
Create `.env.example` at the repo root listing only the three env vars the app code actually references (per Plan 05's `lib/supabase/server.ts`/`client.ts`/`proxy.ts`), using placeholder values only — never real secrets:

If Plan 04 issued publishable/secret naming:
```
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxx
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxxxxxx
```

If Plan 04 issued legacy anon/service_role naming instead:
```
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxxxx...
SUPABASE_SERVICE_ROLE_KEY=eyJxxxxx...
```

Use whichever pair Plan 04's SUMMARY actually recorded — grep `lib/supabase/server.ts` to confirm the exact publishable/anon var name matches before finalizing. The secret/service-role line must never carry a `NEXT_PUBLIC_` prefix (T-01).
  </action>
  <acceptance_criteria>
    - `.env.example` exists with exactly 3 lines (URL, publishable/anon key, secret/service_role key)
    - The publishable/anon key var name in `.env.example` exactly matches the one referenced in `lib/supabase/server.ts`
    - No line in `.env.example` prefixes the secret/service-role var with `NEXT_PUBLIC_`
    - No real secret values appear in the file (only placeholders)
  </acceptance_criteria>
  <verify>
    <automated>K1=$(grep -o 'NEXT_PUBLIC_SUPABASE_[A-Z_]*KEY' lib/supabase/server.ts | head -1); grep -q "$K1" .env.example && ! grep -q 'NEXT_PUBLIC.*SECRET\|NEXT_PUBLIC.*SERVICE_ROLE' .env.example && echo PASS</automated>
  </verify>
  <done>.env.example accurately documents the real env var contract with placeholder values only.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Vercel project, connect Git, wire production env vars, deploy</name>
  <read_first>.env (Plan 04's gitignored file — the real Supabase values to push into Vercel; read locally, never echo full secret values to terminal output/logs beyond what's necessary)</read_first>
  <files>none (Vercel project state, not repo files)</files>
  <action>
Vercel CLI is already authenticated locally as `byagasocial`. Per D-05, create exactly one production project with no preview/staging environment:

```bash
npx vercel link --yes
npx vercel git connect
```

Set the three production env vars using the REAL values from `.env` (written by Plan 04) — read them from the file, do not retype/guess:

```bash
source .env
printf '%s' "$NEXT_PUBLIC_SUPABASE_URL" | npx vercel env add NEXT_PUBLIC_SUPABASE_URL production

# Use whichever var name Plan 04/Task 1 settled on:
printf '%s' "$NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY" | npx vercel env add NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY production
# (or NEXT_PUBLIC_SUPABASE_ANON_KEY, matching .env.example from Task 1)

printf '%s' "$SUPABASE_SECRET_KEY" | npx vercel env add SUPABASE_SECRET_KEY production
# (or SUPABASE_SERVICE_ROLE_KEY, matching .env.example from Task 1)
```

Deploy to production:

```bash
npx vercel --prod
```

Capture the resulting deployment URL from the command output — Task 3 verifies against it.
  </action>
  <acceptance_criteria>
    - `npx vercel project ls` (or `vercel inspect`) shows a project connected to this repo's GitHub remote
    - `npx vercel env ls production` lists exactly 3 env vars: the URL, the publishable/anon key, and the secret/service_role key
    - `npx vercel --prod` completes with a deployment URL printed and exit code 0
  </acceptance_criteria>
  <verify>
    <automated>npx vercel env ls production 2>&1 | grep -c 'NEXT_PUBLIC_SUPABASE\|SUPABASE_' | grep -q '^3$' && echo PASS</automated>
  </verify>
  <done>Vercel project exists, is Git-connected for main-branch-to-production deploys, has exactly the 3 required production env vars set from the real Supabase project, and has a live production deployment.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full DPLY-01 verification — smoke check + secret-leak grep</name>
  <read_first>scripts/smoke-check.sh (Plan 04 output); .env (for the secret value to grep for, without printing it to any committed file)</read_first>
  <files>none (verification only)</files>
  <action>
Run the smoke check against the real deployment URL from Task 2:

```bash
bash scripts/smoke-check.sh "<deployment-url-from-task-2>"
```

Expect: `PASS: <url> returned 200 and contains marker 'ASOBARCELONA'`.

Then run the one-time secret-leak sanity check (T-01 mitigation) against the local production build output — the built `.next/` directory is the same artifact Vercel serves, so grepping it locally is equivalent to checking the deployed bundle:

```bash
npm run build
SECRET_VALUE=$(grep -oP '(?<=SUPABASE_SECRET_KEY=|SUPABASE_SERVICE_ROLE_KEY=).*' .env)
if grep -r "$SECRET_VALUE" .next/ 2>/dev/null; then
  echo "FAIL: secret key value found in build output"
  exit 1
else
  echo "PASS: secret key value not found in .next/ build output"
fi
```

Both checks must pass before this task (and the phase) is considered done.
  </action>
  <acceptance_criteria>
    - `scripts/smoke-check.sh <deployment-url>` prints `PASS`
    - The secret-value grep against `.next/` finds zero matches
    - `curl -I <deployment-url>` returns `HTTP/2 200` (or `HTTP/1.1 200`)
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/smoke-check.sh "$DEPLOY_URL"</automated>
  </verify>
  <done>Deployed production URL renders the ASOBARCELONA shell marker with HTTP 200, and the secret/service-role key is confirmed absent from the built client bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Vercel build/runtime env ↔ client bundle | `NEXT_PUBLIC_*` vars are compiled into the browser bundle; the secret/service-role var must never cross this boundary. |
| Local `.env` ↔ Vercel dashboard | Real secret values move from the local gitignored file into Vercel's env store via `vercel env add`, never via a committed file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Information Disclosure | Service-role/secret key leaked into the client bundle via a wrong env var prefix | mitigate | Task 1 hard-codes the secret var name without a `NEXT_PUBLIC_` prefix in `.env.example` (grep-verified); Task 2 sets it via `vercel env add ... production` only (never `NEXT_PUBLIC_`-prefixed); Task 3 does a concrete post-build grep of `.next/` for the literal secret value and fails the task if found. |
| T-02 | Elevation of Privilege | RLS enabled but with an accidental permissive policy | deferred | Already mitigated live in Plan 04 against the actual database; not re-verified here since this plan touches no schema. |
| T-03 | Tampering/Repudiation | Migration drift via manual dashboard edits | deferred | Already mitigated in Plan 04 via the fresh-project replay; this plan only deploys application code. |
</threat_model>

<verification>
`bash scripts/smoke-check.sh <deploy-url>` passes. Secret-value grep against `.next/` build output returns zero matches. `vercel env ls production` shows exactly 3 vars, none of which prefix the secret key with `NEXT_PUBLIC_`.
</verification>

<success_criteria>
- Vercel production deployment is live, Git-connected to `main` (D-05).
- Env vars match the real Plan 04 Supabase project exactly.
- Deployed shell renders (200 + ASOBARCELONA marker).
- Secret key confirmed absent from the client bundle.
- Phase 1 success criteria (ROADMAP.md) are all met: shell renders, schema is version-controlled with RLS deny-by-default, migrations are reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deployment/01-06-SUMMARY.md`
</output>
