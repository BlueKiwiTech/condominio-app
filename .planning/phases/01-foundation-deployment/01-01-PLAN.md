---
phase: 01-foundation-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - next-env.d.ts
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - eslint.config.mjs
  - public/
  - .gitignore
autonomous: true
requirements: [DPLY-01]

must_haves:
  truths:
    - "A Next.js 16 App Router project exists at the repo root with all Phase 1-8 dependencies installed"
    - "The existing CLAUDE.md, .git history, docs/, and .planning/ are untouched by the scaffold"
    - "`npm run build` succeeds on the vanilla scaffold before any Once UI/next-intl wiring is added"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16.3.x + all Phase 1 dependencies (Once UI, Supabase SSR, next-intl, zod, react-hook-form, date-fns, jose, bcryptjs, classnames)"
      contains: "\"next\":"
    - path: "next.config.ts"
      provides: "Base Next.js config (finalized with Sass/next-intl wiring in Plan 02)"
  key_links:
    - from: "package.json"
      to: "node_modules/@once-ui-system/core"
      via: "npm install"
      pattern: "@once-ui-system/core"
---

<objective>
Scaffold a fresh Next.js 16.3.x App Router project into this already-populated git repository without corrupting existing files (`.git`, `CLAUDE.md`, `docs/`, `.planning/`), then install every dependency this whole phase (and the client-helper/Once UI wiring in the next plan) will need.

Purpose: Everything downstream (Once UI shell, Supabase clients, migrations tooling) needs a working `package.json`/`node_modules` first. This plan produces that foundation only — no Once UI, no next-intl, no Supabase code yet (those are Plan 02 and Plan 05).
Output: A buildable, empty-shell Next.js 16 app at the repo root with all required npm packages installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-deployment/01-RESEARCH.md
@.planning/phases/01-foundation-deployment/01-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 into a scratch dir and merge into the repo root</name>
  <read_first>
    CLAUDE.md (to confirm the exact unique content that must survive the merge unchanged — the "ASOBARCELONA" project description and the Technology Stack section)
    .gitignore (current repo root file — already covers `/node_modules`, `/.next/`, `.env*.local`, `.env`, `.vercel`, `*.tsbuildinfo`, `next-env.d.ts`; confirmed via direct read, no merge needed if create-next-app's generated `.gitignore` is excluded from the rsync)
  </read_first>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, next-env.d.ts, app/layout.tsx, app/page.tsx, app/globals.css, eslint.config.mjs, public/*</files>
  <action>
Do NOT run `create-next-app .` directly at the repo root — it has a documented history of `EACCES`/`EPERM` failures scanning `.git/objects`, and its `--agents-md` default (now on by default) would overwrite `CLAUDE.md`. Scaffold into the scratchpad dir and merge in:

```bash
cd "$SCRATCHPAD_DIR"
npx create-next-app@latest condominio-scaffold \
  --typescript --app --no-tailwind --turbopack --eslint \
  --import-alias "@/*" --no-agents-md --use-npm --disable-git

rsync -av --exclude='.git' --exclude='CLAUDE.md' --exclude='.gitignore' \
  "$SCRATCHPAD_DIR/condominio-scaffold/" /Users/gabrielvega/Projects/agasocial/renata/condominio-app/
```

After the merge, confirm the repo's existing `.gitignore` already covers everything `create-next-app`'s own `.gitignore` would add (it does — verified: `/node_modules`, `/.next/`, `.env*.local`, `.env`, `.vercel`, `*.tsbuildinfo`, `next-env.d.ts` are all already present). Do not overwrite it; leave the existing file as-is.

Required flags and why: `--no-tailwind` (Once UI uses Sass, not Tailwind — CLAUDE.md constraint), `--no-agents-md` (prevents `CLAUDE.md` from being overwritten), `--disable-git` (the scratch scaffold must not create its own `.git`, since we're merging into an existing repo), `--import-alias "@/*"` (matches every `@/...` import path used throughout RESEARCH.md's code blocks).
  </action>
  <acceptance_criteria>
    - `test -f package.json` at repo root succeeds and `grep -q '"next"' package.json` matches a `16.` version
    - `! test -f tailwind.config.ts` (no Tailwind config was generated)
    - `grep -q ASOBARCELONA CLAUDE.md` still matches (file untouched by the merge)
    - `git status --short .git` is not itself modified (the scaffold never touched `.git`)
    - `.gitignore` still contains `/node_modules`, `/.next/`, `.env`, `.vercel` (unchanged)
  </acceptance_criteria>
  <verify>
    <automated>test -f package.json && grep -q '"next"' package.json && ! test -f tailwind.config.ts && grep -q ASOBARCELONA CLAUDE.md && echo PASS</automated>
  </verify>
  <done>Next.js 16 App Router scaffold exists at repo root; CLAUDE.md, .git, docs/, .planning/ are byte-identical to before this task; no tailwind.config.ts exists.</done>
</task>

<task type="auto">
  <name>Task 2: Install all Phase 1+ dependencies and verify the vanilla build</name>
  <read_first>package.json (written by Task 1, to confirm which deps are already present from create-next-app before adding more)</read_first>
  <files>package.json, package-lock.json</files>
  <action>
From the repo root, install every package this phase and the immediately-following Once UI/Supabase wiring plans need in one pass (installing them all now avoids a second `npm install` round-trip mid-phase):

```bash
npm install @supabase/ssr @supabase/supabase-js @once-ui-system/core \
  zod react-hook-form @hookform/resolvers date-fns next-intl jose bcryptjs classnames
npm install sass sharp
npm install -D @types/bcryptjs
```

Note on `classnames`: RESEARCH.md's Pattern 2 code block (`app/[locale]/layout.tsx`) imports `classNames` from the `classnames` package but the Standard Stack table omits it — this is a corrected gap; install it explicitly here or Plan 02's layout will fail to build with a "module not found" error.

Do NOT install `react-icons` directly — per CLAUDE.md, Once UI (`@once-ui-system/core`) bundles it as a transitive dependency; Plan 02's `resources/icons.ts` imports directly from `react-icons/hi2` and relies on npm's dependency hoisting, matching Once UI's own starter convention (confirmed via Context7 `/once-ui-system/nextjs-starter`).

After install, run the build against the still-vanilla (pre-Once-UI) scaffold to catch any base config issues early, before Once UI/next-intl complexity is layered on in Plan 02:

```bash
npm run build
```
  </action>
  <acceptance_criteria>
    - `package.json` `dependencies` include: `@supabase/ssr`, `@supabase/supabase-js`, `@once-ui-system/core`, `zod`, `react-hook-form`, `@hookform/resolvers`, `date-fns`, `next-intl`, `jose`, `bcryptjs`, `classnames`, `sass`, `sharp`
    - `package.json` `devDependencies` include `@types/bcryptjs`
    - `npm run build` exits 0
    - No `tailwind` string appears anywhere in `package.json`
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All required npm packages are installed and `npm run build` succeeds on the vanilla (pre-Once-UI, pre-next-intl) scaffold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A this plan | No env vars, no network-facing code, no schema — pure local scaffold + dependency install. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01 | Information Disclosure | Service-role/secret key leaked via wrong env var prefix | deferred | Not applicable in this plan — no env vars are introduced yet. Mitigated in Plan 06 (Vercel env wiring + `.next/` build-output grep). |
| T-02 | Elevation of Privilege | RLS enabled but with an accidental permissive policy | deferred | Not applicable — no schema exists yet. Mitigated in Plan 03 (schema authoring) and verified in Plan 04. |
| T-03 | Tampering/Repudiation | Migration drift via manual dashboard edits | deferred | Not applicable — no Supabase project exists yet. Mitigated in Plan 04 (fresh-project migration replay). |
</threat_model>

<verification>
Run `npm run build` — must exit 0. Confirm CLAUDE.md, .git, docs/, .planning/ are unchanged (`git status --short` should show only newly-added scaffold files as untracked/added, never a modification to CLAUDE.md itself).
</verification>

<success_criteria>
- Next.js 16.3.x App Router project scaffolded at repo root, buildable.
- All Phase 1 dependencies (Once UI, Supabase SSR, next-intl, zod/RHF/resolvers, date-fns, jose, bcryptjs, classnames, sass, sharp) installed.
- No Tailwind config, no `middleware.ts` (create-next-app's App Router template doesn't generate one).
- CLAUDE.md, .git, docs/, .planning/ untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-deployment/01-01-SUMMARY.md`
</output>
