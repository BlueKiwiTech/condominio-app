---
phase: 01-foundation-deployment
plan: 01
subsystem: infra
tags: [nextjs, npm, scaffold, once-ui, supabase, next-intl, turbopack]

# Dependency graph
requires: []
provides:
  - Buildable Next.js 16.3.4 App Router scaffold at repo root
  - All Phase 1+ npm dependencies installed (Once UI, Supabase SSR, next-intl, zod/RHF, date-fns, jose, bcryptjs, classnames, sass, sharp)
affects: [01-02 (Once UI + next-intl wiring), 01-03 (schema/migrations), 01-05 (Supabase clients), 01-06 (Vercel deployment)]

# Tech tracking
tech-stack:
  added: [next@16.3.4, typescript, eslint, "@supabase/ssr", "@supabase/supabase-js", "@once-ui-system/core", zod, react-hook-form, "@hookform/resolvers", date-fns, next-intl, jose, bcryptjs, classnames, sass, sharp]
  patterns: ["Scaffold-into-scratchpad-then-rsync-merge to avoid create-next-app colliding with an existing git repo"]

key-files:
  created: [package.json, package-lock.json, tsconfig.json, next.config.ts, eslint.config.mjs, app/layout.tsx, app/page.tsx, app/page.module.css, app/globals.css, app/favicon.ico, public/file.svg, public/globe.svg, public/next.svg, public/vercel.svg, public/window.svg, README.md]
  modified: []

key-decisions:
  - "Excluded node_modules/ and .next/ from the scratchpad-to-repo rsync (not in the plan's rsync command literally, but both are regenerated by the Task 2 npm install/build and were never meant to be merged verbatim) — kept the merge diff limited to source/config files."
  - "Also merged README.md, app/favicon.ico, and app/page.module.css even though they weren't listed in the plan's <files> block — page.module.css is a hard import dependency of app/page.tsx and omitting it would break the build; README.md/favicon.ico are harmless standard scaffold output."

requirements-completed: [DPLY-01]

# Metrics
duration: 5min
completed: 2026-09-05
---

# Phase 1 Plan 1: Scaffold & Dependencies Summary

**Next.js 16.3.4 App Router scaffold merged into the existing repo via scratchpad+rsync, with all Phase 1+ dependencies (Once UI, Supabase SSR, next-intl, zod/RHF, jose, bcryptjs) installed and a clean vanilla `npm run build`.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-09-05T19:59:00-05:00 (approx, scratchpad scaffold run)
- **Completed:** 2026-09-05T20:01:25-05:00
- **Tasks:** 2 completed
- **Files modified:** 18 (16 created in Task 1, package.json/package-lock.json updated in Task 2)

## Accomplishments
- Next.js 16.3.4 App Router project scaffolded at the repo root without disturbing `.git`, `CLAUDE.md`, `docs/`, `.planning/`, or the existing `.gitignore`
- No `tailwind.config.ts` generated (Once UI uses Sass, per CLAUDE.md constraint)
- All Phase 1+ npm dependencies installed in one pass (Once UI, Supabase SSR, next-intl, zod/RHF/resolvers, date-fns, jose, bcryptjs, classnames, sass, sharp, @types/bcryptjs)
- `npm run build` succeeds (exit 0) on the vanilla, pre-Once-UI, pre-next-intl scaffold

## Task Commits

Each task was committed atomically:

1. **Task 1: Scaffold Next.js 16 into a scratch dir and merge into the repo root** - `7df1c57` (feat)
2. **Task 2: Install all Phase 1+ dependencies and verify the vanilla build** - `0ba3e55` (feat)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created/Modified
- `package.json` - Next.js 16.3.4 project manifest, all Phase 1+ deps added
- `package-lock.json` - Lockfile for the above
- `tsconfig.json` - TypeScript config with `@/*` import alias (create-next-app default)
- `next.config.ts` - Base Next.js config (finalized with Sass/next-intl wiring in Plan 02)
- `eslint.config.mjs` - ESLint flat config (create-next-app default)
- `app/layout.tsx`, `app/page.tsx`, `app/page.module.css`, `app/globals.css`, `app/favicon.ico` - Placeholder App Router shell (will be replaced/wrapped by Plan 02's `[locale]` segment + Once UI Providers)
- `public/*.svg` - create-next-app default static assets
- `README.md` - create-next-app default readme

## Decisions Made
- Excluded `node_modules/` and `.next/` from the scaffold→repo rsync merge — the plan's rsync example only explicitly excludes `.git`/`CLAUDE.md`/`.gitignore`, but copying ~280 packages of `node_modules` and build cache verbatim from the scratchpad made no sense given Task 2 runs a fresh `npm install` at the repo root anyway. This kept the git diff limited to actual source/config files.
- Kept `README.md`, `app/favicon.ico`, and `app/page.module.css` in the merge even though they weren't itemized in the plan's `<files>` list — `page.module.css` is a direct `import` in the generated `app/page.tsx` and omitting it would have broken the build; the other two are inert standard scaffold output.
- Did not rename the `package.json` `name` field away from the create-next-app default (`condominio-scaffold`) — cosmetic only, not in scope for this plan's acceptance criteria; can be renamed in a later plan if desired.

## Deviations from Plan

None - plan executed exactly as written (the two minor merge-scope notes above are clarifications of the literal rsync command, not deviations from the plan's intent or acceptance criteria — all acceptance criteria for both tasks passed as specified).

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required in this plan (Supabase/Vercel setup is Plans 01-04/01-06).

## Next Phase Readiness

- `package.json`/`node_modules` foundation is in place; Plan 02 (Once UI + next-intl wiring) can proceed immediately.
- No blockers. Vanilla build is green, confirming the base Next.js config has no pre-existing issues before Once UI/next-intl complexity is layered on.

---
*Phase: 01-foundation-deployment*
*Completed: 2026-09-05*

## Self-Check: PASSED

All created files verified present on disk (package.json, package-lock.json, tsconfig.json, next.config.ts, eslint.config.mjs, app/layout.tsx, app/page.tsx, app/page.module.css, app/globals.css, app/favicon.ico, public/file.svg, README.md). Both task commits (`7df1c57`, `0ba3e55`) verified present in `git log`.
